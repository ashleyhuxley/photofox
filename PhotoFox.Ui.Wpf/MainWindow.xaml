<Window x:Class="PhotoFox.Ui.Wpf.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:PhotoFox.Wpf.Ui.Mvvm.ViewModels;assembly=PhotoFox.Wpf.Ui.Mvvm"
        d:DataContext="{d:DesignInstance vm:MainWindowViewModel}"
        mc:Ignorable="d"
        Loaded="Window_Loaded"
        Title="PhotoFox" Height="450" Width="800"
        SizeChanged="Window_SizeChanged">
    <DockPanel Margin="10">
        <Ribbon DockPanel.Dock="Top">
            <RibbonTab Header="Photos">
                <RibbonGroup>
                    <RibbonButton Label="Add Photos" LargeImageSource="/Images/add.png" Command="{Binding AddPhotosCommand}" />
                </RibbonGroup>
            </RibbonTab>

        </Ribbon>

        <TabControl>
            <TabItem Header="Photos">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="3*" />
                        <ColumnDefinition Width="5" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                
                    <ListView 
                        x:Name="PhotoList" 
                        HorizontalAlignment="Stretch" 
                        VerticalAlignment="Stretch" 
                        ScrollViewer.VerticalScrollBarVisibility="Visible" 
                        ScrollViewer.ScrollChanged="PhotoList_ScrollChanged" 
                        ItemsSource="{Binding Photos}"
                        SelectedItem="{Binding SelectedPhoto}">
                        <ListView.ItemContainerStyle>
                            <Style TargetType="ListBoxItem">
                                <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                            </Style>
                        </ListView.ItemContainerStyle>
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <UniformGrid Columns="6" HorizontalAlignment="Stretch" />
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                        <ListView.ItemTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Vertical" VerticalAlignment="Stretch" HorizontalAlignment="Center" Width="Auto">
                                    <Image Source="{Binding Image}" HorizontalAlignment="Center" VerticalAlignment="Top" Width="150" />
                                    <TextBlock Text="{Binding Title}" HorizontalAlignment="Center" VerticalAlignment="Bottom" />
                                </StackPanel>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                        <ListView.GroupStyle>
                            <GroupStyle>
                                <GroupStyle.HeaderTemplate>
                                    <DataTemplate>
                                        <Grid Background="CornflowerBlue" Margin="0,10,0,5">
                                            <TextBlock FontWeight="Bold" FontSize="14" Margin="4" Text="{Binding Name}"/>
                                        </Grid>
                                    </DataTemplate>
                                </GroupStyle.HeaderTemplate>
                            </GroupStyle>
                        </ListView.GroupStyle>
                    </ListView>

                    <GridSplitter Grid.Column="1" Width="5" HorizontalAlignment="Stretch" />

                    <StackPanel Orientation="Vertical" Grid.Column="2">
                        <Label FontWeight="Bold">Title</Label>
                        <TextBox Text="{Binding SelectedPhoto.Title}" Margin="10,0,0,0" />
                        <Label FontWeight="Bold">Description</Label>
                        <TextBox Text="{Binding SelectedPhoto.Description}" Margin="10,0,0,0" />
                        <Label FontWeight="Bold">Date Taken</Label>
                        <TextBlock Text="{Binding SelectedPhoto.DateTaken}" Margin="10,0,0,0" />
                        <Label FontWeight="Bold">Device</Label>
                        <TextBlock Text="{Binding SelectedPhoto.Device}" Margin="10,0,0,0" />
                        <Label FontWeight="Bold">Aperture</Label>
                        <TextBlock Text="{Binding SelectedPhoto.Apeture}" Margin="10,0,0,0" />
                        <Label FontWeight="Bold">Exposure</Label>
                        <TextBlock Text="{Binding SelectedPhoto.Exposure}" Margin="10,0,0,0" />
                        <Label FontWeight="Bold">GPS Location</Label>
                        <Button Margin="10,0,0,0" Content="{Binding SelectedPhoto.GpsCoords}" Cursor="Hand" Command="{Binding OpenGpsLink}">
                            <Button.Template>
                                <ControlTemplate TargetType="Button">
                                    <TextBlock TextDecorations="Underline">
                                        <ContentPresenter />
                                    </TextBlock>
                                </ControlTemplate>
                            </Button.Template>
                            <Button.Style>
                                <Style TargetType="Button">
                                    <Setter Property="Foreground" Value="Blue" />
                                    <Style.Triggers>
                                        <Trigger Property="IsMouseOver" Value="true">
                                            <Setter Property="Foreground" Value="Red" />
                                        </Trigger>
                                    </Style.Triggers>
                                </Style>
                            </Button.Style>
                        </Button>
                        <Button Width="120" Height="40" HorizontalAlignment="Left" Margin="10,20,0,0">
                            <StackPanel Orientation="Horizontal">
                                <Image />
                                <TextBlock>Save Changes</TextBlock>
                            </StackPanel>
                        </Button>
                    </StackPanel>
                </Grid>
            </TabItem>
            <TabItem Header="Albums">
                <ListView x:Name="AlbumList" HorizontalAlignment="Stretch" VerticalAlignment="Top" ItemsSource="{Binding Albums}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <UniformGrid Columns="5" HorizontalAlignment="Stretch" />
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Vertical" VerticalAlignment="Stretch" HorizontalAlignment="Stretch">
                                <Image Source="{Binding Image}" HorizontalAlignment="Stretch" VerticalAlignment="Top" />
                                <TextBlock Text="{Binding Title}" HorizontalAlignment="Stretch" VerticalAlignment="Bottom" />
                            </StackPanel>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </TabItem>
        </TabControl>
    </DockPanel>
</Window>
